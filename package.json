{
  "name": "slush-revelry",
  "version": "1.0.0",
  "description": "A generator for Revelry Node.js apps that use React, Redux, and more.",
  "main": "index.js",
  "scripts": {
    "test:install": "npm install gulp slush . -g",
    "test:new": "slush revelry:new test",
    "test:module": "(cd ./test; slush revelry:module widgets)",
    "test:start": "(cd ./test; npm install && npm run build && npm test && gulp dev)",
    "test:clean": "rm -rf ./test",
    "test": "npm run test:clean; npm run test:install && npm run test:new && npm run test:module && npm run test:start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-conflict": "^0.4.0",
    "gulp-install": "^0.6.0",
    "gulp-rename": "^1.2.2",
    "gulp-template": "^4.0.0",
    "lodash": "^4.17.2",
    "lodash-inflection": "^1.5.0"
  }
}
